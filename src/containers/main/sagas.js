
/* Import Third Party Dependencies */
import { takeLatest, put, select, call } from 'redux-saga/effects';


function* getData() {}

export default function* defaultSaga () {
  // yield takeLatest(SOME_THING, getData);
}